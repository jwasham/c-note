#include <stdio.h>

#define MAX_LENGTH 500

int main()
{
    int i;
    char test[MAX_LENGTH];

    for (i = 0; i < MAX_LENGTH - 1 ; i++) {
        test[i] = ' ';
    }

    // in progress

    return 0;
}